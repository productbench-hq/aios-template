# AGENTS.md

This kit is written for Claude Code and Codex. The operating manual lives in `CLAUDE.md`, so there is one source of truth.

- **Session start:** read `CLAUDE.md` and follow it as if it were this file.
- **Skills:** live in `.claude/skills/<name>/SKILL.md`. When the user runs or asks for `/<name>` (e.g. `/onboard`, `/weekly-review`), read that file and follow its steps.
- **Editing the manual:** change `CLAUDE.md`, never this file.
